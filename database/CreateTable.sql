Create table Groupe(
id SERIAL PRIMARY KEY
);

Create table User(
id SERIAL PRIMARY KEY
);

Create table Recette(
id SERIAL PRIMARY KEY
);

Create table CourseRaw(
id SERIAL PRIMARY KEY
);

Create table Recette(
id SERIAL PRIMARY KEY
);

Create table Ingredient(
id SERIAL PRIMARY KEY
);